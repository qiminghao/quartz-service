package com.example.quartzservice.mq.receiver;

import javax.jms.Message;
import javax.jms.MessageListener;

public class CommonMessageListener implements MessageListener {
    @Override
    public void onMessage(Message message) {

    }
}
